1. what's %token and %type mean in hoc.y
- what's "return 1" means in list definition // DONE: return 0 means the input file of yyparse is finished.
- what's the $$ = XXX in hoc.y means. why introduce 'begin' when support function.
- add comment support // DONE
- the local variable and global variable // DONE

- named function parameter
- output each line, and show instrument under it.
- the lineno is incorrect when parse error
